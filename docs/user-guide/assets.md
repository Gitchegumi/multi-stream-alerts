# Assets

Assets are workspace-owned images, videos, and audio files used by canvases.

GitchAlerts supports local/server uploads, external URLs when enabled, and object-storage-backed assets when configured. Asset access is scoped to the workspace; overlay browser sources can load protected assets through display-key-aware URLs.

Use short, descriptive filenames so layout editing remains easy as your library grows.
