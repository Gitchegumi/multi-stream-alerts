# Account Settings

Account and workspace administration lives in **Settings**.

Workspace settings control the channel display name. Integration settings are nested under Settings and hold provider credentials for Ko-fi, Twitch, and YouTube.

Only users with sufficient workspace permissions can change settings or provider credentials.
