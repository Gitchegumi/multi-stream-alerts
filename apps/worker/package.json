{
  "name": "@multi-stream-alerts/worker",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "tsx watch src/worker.ts",
    "lint": "tsc --noEmit",
    "start": "tsx src/worker.ts",
    "test": "node --import tsx --test $(find src -path '*/__tests__/*.test.ts' -type f)",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@multi-stream-alerts/database": "workspace:*",
    "@multi-stream-alerts/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
